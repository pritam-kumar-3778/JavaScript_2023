const arr = [1,2,3,4];
// we can perform operation on constant array but we can not create a new array as same name.
console.log(arr);
arr.push(5);
console.log(arr);

//arr = [1,2,3,4,"abc"]; It throws an error.