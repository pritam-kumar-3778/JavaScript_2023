//Take the user's name & age as input using prompts.
//Then return back the following statement to the user as an alert(by substituting their name & age) :
//name has age yeas old.
//[Use templat eLiterals to print this sentence]

let name = prompt("Enter user's name :");
let age = prompt("Enter user's age :");

alert(`${name} "has" ${age} "years old."`);