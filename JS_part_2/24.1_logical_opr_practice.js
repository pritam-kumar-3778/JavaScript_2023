//A "good string" is a string that start with letter a & has a length >3. 
//Write a program to find if a string is good or not 
let str = "apple";

if((str[0] === 'a') && (str.length > 3)){
    console.log("good string")
}else{
    console.log("Not a good string");
}
