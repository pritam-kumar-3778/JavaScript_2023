// Create a traffic light system that shows what to do based on color
let color = "red";

//traffc light system
if(color == "red"){
    console.log(`stop due to ${color} colour`);
}
if(color == "yellow"){
    console.log("Drive slow due to yellow colour");
}
if(color == "green"){
    console.log("Go due to green colour");
}